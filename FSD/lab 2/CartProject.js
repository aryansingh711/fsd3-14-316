import readline from "readline/promises";
import { stdin, stduout } from "process";

const main = async () => {
  let choice = ;
  const updatequantity = async (pid) => {
    const cart = await getcart();
    const product =
  }
  }
  